# PUBLIC_INTERFACE
# API base URL used by the Angular frontend to connect to the backend REST API.
# The orchestrator will map this into the runtime environment.
API_BASE_URL=https://your-backend.example.com/api
